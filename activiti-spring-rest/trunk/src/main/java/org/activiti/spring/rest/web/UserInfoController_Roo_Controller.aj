// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.activiti.spring.rest.web;

import java.io.UnsupportedEncodingException;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.activiti.spring.rest.model.UserInfo;
import org.activiti.spring.rest.model.UserRecord;
import org.activiti.spring.rest.web.UserInfoController;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect UserInfoController_Roo_Controller {
    
    @RequestMapping(method = RequestMethod.POST, produces = "text/html")
    public String UserInfoController.create(@Valid UserInfo userInfo, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, userInfo);
            return "userinfoes/create";
        }
        uiModel.asMap().clear();
        userInfo.persist();
        return "redirect:/userinfoes/" + encodeUrlPathSegment(userInfo.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", produces = "text/html")
    public String UserInfoController.createForm(Model uiModel) {
        populateEditForm(uiModel, new UserInfo());
        return "userinfoes/create";
    }
    
    @RequestMapping(value = "/{id}", produces = "text/html")
    public String UserInfoController.show(@PathVariable("id") String id, Model uiModel) {
        uiModel.addAttribute("userinfo", UserInfo.findUserInfo(id));
        uiModel.addAttribute("itemId", id);
        return "userinfoes/show";
    }
    
    @RequestMapping(produces = "text/html")
    public String UserInfoController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("userinfoes", UserInfo.findUserInfoEntries(firstResult, sizeNo));
            float nrOfPages = (float) UserInfo.countUserInfoes() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("userinfoes", UserInfo.findAllUserInfoes());
        }
        return "userinfoes/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT, produces = "text/html")
    public String UserInfoController.update(@Valid UserInfo userInfo, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, userInfo);
            return "userinfoes/update";
        }
        uiModel.asMap().clear();
        userInfo.merge();
        return "redirect:/userinfoes/" + encodeUrlPathSegment(userInfo.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", produces = "text/html")
    public String UserInfoController.updateForm(@PathVariable("id") String id, Model uiModel) {
        populateEditForm(uiModel, UserInfo.findUserInfo(id));
        return "userinfoes/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE, produces = "text/html")
    public String UserInfoController.delete(@PathVariable("id") String id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        UserInfo userInfo = UserInfo.findUserInfo(id);
        userInfo.remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/userinfoes";
    }
    
    void UserInfoController.populateEditForm(Model uiModel, UserInfo userInfo) {
        uiModel.addAttribute("userInfo", userInfo);
        uiModel.addAttribute("userrecords", UserRecord.findAllUserRecords());
    }
    
    String UserInfoController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        } catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
