
# TODO This is supposed to read the props into Spring context (and it seems it does) but still don't get them set on the @Value annotations
#    <bean id="propertyConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
#      <property name="locations">
#        <list>
#          <value>classpath*:META-INF/spring/activiti-spring-rest.properties</value>
#          <value>/WEB-INF/spring/activiti-spring-rest.properties</value>
#        </list>
#      </property>
#    </bean>


# null is how file protocols present themselves in CORS requests. 
# localhost is a convenience to devs
# chrome extension is a REST testing tool (Postman)  
# TODO migrate away from cors.properties
allowed.origins=null,http://localhost:8080,chrome-extension://fdmmgilgnpjigdojojpjoooidkmcomcm
message.allowAnonymous=true